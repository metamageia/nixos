apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: my-config
  namespace: flux-system
spec:
  interval: 1m
  prune: true
  path: "./clusters/auriga/apps" 
  sourceRef:
    kind: GitRepository
    name: my-config
  targetNamespace: default